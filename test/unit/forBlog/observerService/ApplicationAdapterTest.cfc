import cfml.forBlog.observerService.ObserverService

component extends=Testbox.system.BaseSpec {
    function run() {
        describe("Tests for ApplicationAdapter", () => {
            describe("Tests for set method", () => {
                it("tests nothing yet", () => {
                    expect(true).toBeFalse()
                })
            })
        })
    }

}
